import React from "react";

const HomePageTitle = ({ title }) => {
  return (
    <div className=" bg-black text-4xl font-bold text-white w-full p-4">
      {title}
    </div>
  );
};

export default HomePageTitle;
